<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TRMNL Preview</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      background: #1a1a1a;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 20px;
      font-family: system-ui, -apple-system, sans-serif;
    }

    .preview-label {
      color: #666;
      font-size: 14px;
      margin-bottom: 12px;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .device-frame {
      background: #2a2a2a;
      padding: 20px;
      border-radius: 12px;
      box-shadow: 0 20px 60px rgba(0,0,0,0.5);
    }

    .screen {
      width: 800px;
      height: 480px;
      background: #f5f5f0;
      border-radius: 4px;
      padding: 24px;
      display: flex;
      flex-direction: column;
      font-family: 'Inter', 'SF Pro Display', system-ui, sans-serif;
      color: #111;
      overflow: hidden;
    }

    .columns {
      display: flex;
      gap: 32px;
      flex: 1;
      min-height: 0;
    }

    .column {
      flex: 1;
      display: flex;
      flex-direction: column;
      min-height: 0;
    }

    .section-header {
      font-size: 18px;
      font-weight: 600;
      padding-bottom: 8px;
      border-bottom: 2px solid #111;
      margin-bottom: 12px;
      display: flex;
      justify-content: space-between;
      align-items: baseline;
    }

    .section-count {
      font-size: 14px;
      font-weight: 400;
      color: #555;
    }

    .todo-list {
      display: flex;
      flex-direction: column;
      gap: 8px;
      overflow: hidden;
    }

    .todo-item {
      display: flex;
      gap: 10px;
      align-items: flex-start;
    }

    .todo-checkbox {
      width: 16px;
      height: 16px;
      border: 2px solid #333;
      border-radius: 3px;
      flex-shrink: 0;
      margin-top: 2px;
    }

    .todo-checkbox.checked {
      background: #333;
      position: relative;
    }

    .todo-checkbox.checked::after {
      content: "";
      position: absolute;
      left: 4px;
      top: 1px;
      width: 4px;
      height: 8px;
      border: solid #f5f5f0;
      border-width: 0 2px 2px 0;
      transform: rotate(45deg);
    }

    .todo-item.completed .todo-title {
      text-decoration: line-through;
      color: #666;
    }

    .todo-item.completed .todo-context {
      text-decoration: line-through;
    }

    .todo-content {
      flex: 1;
      min-width: 0;
    }

    .todo-title {
      font-size: 15px;
      font-weight: 500;
      line-height: 1.3;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }

    .todo-context {
      font-size: 12px;
      color: #666;
      margin-top: 2px;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }

    .empty-state {
      color: #888;
      font-size: 14px;
      font-style: italic;
    }

    .footer {
      text-align: center;
      font-size: 11px;
      color: #888;
      padding-top: 12px;
      border-top: 1px solid #ddd;
      margin-top: auto;
    }

    .refresh-hint {
      color: #888;
      font-size: 12px;
      margin-top: 16px;
    }

    .refresh-hint a {
      color: #888;
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="preview-label">TRMNL Preview (800x480)</div>

  <div class="device-frame">
    <div class="screen">
      <div class="columns">
        <!-- Today Column -->
        <div class="column">
          <div class="section-header">
            Today
            <span class="section-count"><%= @counts[:today] %> remaining</span>
          </div>
          <div class="todo-list">
            <% if @todos[:today].any? %>
              <% @todos[:today].each do |todo| %>
                <div class="todo-item">
                  <div class="todo-checkbox"></div>
                  <div class="todo-content">
                    <div class="todo-title"><%= todo[:title] %></div>
                    <% if todo[:project] || todo[:milestone] %>
                      <div class="todo-context">
                        <%= [todo[:project], todo[:milestone]].compact.join(" / ") %>
                      </div>
                    <% end %>
                  </div>
                </div>
              <% end %>
            <% else %>
              <div class="empty-state">All clear!</div>
            <% end %>
          </div>

          <% if @todos[:completed_today].any? %>
            <div class="section-header" style="margin-top: 16px; font-size: 14px;">
              Done
              <span class="section-count"><%= @counts[:completed_today] %> completed</span>
            </div>
            <div class="todo-list">
              <% @todos[:completed_today].each do |todo| %>
                <div class="todo-item completed">
                  <div class="todo-checkbox checked"></div>
                  <div class="todo-content">
                    <div class="todo-title"><%= todo[:title] %></div>
                    <% if todo[:project] || todo[:milestone] %>
                      <div class="todo-context">
                        <%= [todo[:project], todo[:milestone]].compact.join(" / ") %>
                      </div>
                    <% end %>
                  </div>
                </div>
              <% end %>
            </div>
          <% end %>
        </div>

        <!-- Tomorrow Column -->
        <div class="column">
          <div class="section-header">
            Tomorrow
            <span class="section-count"><%= @counts[:tomorrow] %> tasks</span>
          </div>
          <div class="todo-list">
            <% if @todos[:tomorrow].any? %>
              <% @todos[:tomorrow].each do |todo| %>
                <div class="todo-item">
                  <div class="todo-checkbox"></div>
                  <div class="todo-content">
                    <div class="todo-title"><%= todo[:title] %></div>
                    <% if todo[:project] || todo[:milestone] %>
                      <div class="todo-context">
                        <%= [todo[:project], todo[:milestone]].compact.join(" / ") %>
                      </div>
                    <% end %>
                  </div>
                </div>
              <% end %>
            <% else %>
              <div class="empty-state">Nothing yet</div>
            <% end %>
          </div>
        </div>
      </div>

      <div class="footer">
        Updated <%= @generated_at.strftime("%b %d, %H:%M") %>
      </div>
    </div>
  </div>

  <div class="refresh-hint">
    <a href="<%= trmnl_preview_path %>">Refresh</a> to see latest todos
  </div>
</body>
</html>

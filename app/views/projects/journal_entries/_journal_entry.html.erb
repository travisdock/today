<div class="rounded-xl bg-slate-50 p-4">
  <% if journal_entry.image.attached? %>
    <div class="mb-3">
      <%= image_tag journal_entry.image.variant(resize_to_limit: [800, 800], format: :webp), class: "rounded-lg max-w-full max-h-96 object-contain" %>
    </div>
  <% end %>
  <% if journal_entry.content.present? %>
    <p class="text-slate-700 whitespace-pre-wrap"><%= journal_entry.content %></p>
  <% end %>
  <p class="mt-2 text-xs text-slate-400">
    <%= time_ago_in_words(journal_entry.created_at) %> ago
  </p>
</div>
